{
  "name": "freerobotstore-platform-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "SDK + CLI + compliance + host for robot software on freerobotstore.online",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/freerobotstore-online/platform.git"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
